// Methods

// Add food to item to Foods database
// - if item exists, do not add food and return false
export const addFood = async (food) => {
    
}

// Add review to food in the Foods database
// - if food does not exist, return false
// - if user review already exist today, return false
// Add short form review to the Reviews database
export const addReview = async (review, food) => {
    
}

// 
